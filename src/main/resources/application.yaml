server:
  port: 8888

spring:
  application:
    name: restaurant-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Santu-developer/restaurant-config-repo
          default-label: main
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"